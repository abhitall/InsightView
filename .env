# For AWS S3:
# - AWS_REGION must match your bucket's region (e.g., us-east-2)
# - Do NOT set S3_ENDPOINT for AWS S3
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-2
S3_BUCKET=test-bucket

# Remove or comment out these for AWS S3:
# S3_ENDPOINT=http://localhost:9000
# S3_FORCE_PATH_STYLE=true
# S3_TLS_VERIFY=false

PROMETHEUS_PUSHGATEWAY=http://localhost:9091
TEST_URL=http://example.com

# Optional MinIO / S3-compatible configuration
# Uncomment and set these if you are using MinIO or another S3-compatible service
# MINIO_ENDPOINT=https://minio.example.com
# MINIO_ACCESS_KEY=minioaccess
# MINIO_SECRET_KEY=miniosecret
# MINIO_ROOT_USER=minioroot
# MINIO_ROOT_PASSWORD=miniorootpassword
#
# If using a non-AWS S3 compatible endpoint, set S3_ENDPOINT instead of MINIO_ENDPOINT
# S3_ENDPOINT=https://s3-compatible.example.com
# S3_FORCE_PATH_STYLE=true   # Usually true for MinIO
# S3_TLS_VERIFY=false       # Set to false if using self-signed certs

# Example: Prometheus-only configuration (no S3 export)
# PROMETHEUS_PUSHGATEWAY=https://prometheus.example.com:9091
# TEST_URL=https://myapp.example.com
